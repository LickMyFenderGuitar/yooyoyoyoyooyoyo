static mod system<extern: "system">;

fn main() {
    static in user_input = system.in();
    static out string_variable = "hello from Flux!";

    system.println("hello, world!" + string_variable);
}

"main" extends system::autom;
